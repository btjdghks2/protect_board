{{>layout/header}}

{{#board}}
<div class="detailCss">
    <h1>{{title}}</h1>
    <hr>
    <h3>{{content}}</h3>
</div>

<a href="/detail/edit/{{id}}"  class="btn btn-primary">Edit</a>
<a href="/detail/delete/{{id}}"  class="btn btn-danger">Delete</a>
{{/board}}
<a href="/" class="btn btn-danger">back</a>
<div>
<table class="table table-horizontal table-bordered">
    <thead class="thead-strong">
    <tr>
        <th>유저넘버</th>
        <th>제목</th>

    </tr>
    </thead>
    <tbody id="tbody">
    {{#comment}}
        <tr>
            <td>{{id}}</td>
            <td><a href="/detail/{{id}}">{{commentcontent}}</a></td>
        </tr>
    {{/comment}}
    </tbody>
</table>
    <div class="form-group">
        <label for="content"> 내용 </label>
        <textarea class="form-control" id="commentcontent" placeholder="내용을 입력하세요"></textarea>
        <button type="button" class="btn btn-primary" id="btn-comment-save">댓글 등록</button>
    </div>

</div>



{{>layout/footer}}